import React, { useState } from 'react'
import axios from 'axios';

export const ReservationCard = ({reservationId}) => {

  const [currentHotel, setCurrentHotel] = useState(null);

   //fetch data


   //useEffect

  return (
    <div>ReservationCard</div>
  )
}
