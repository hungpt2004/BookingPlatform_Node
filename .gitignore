# Bỏ qua thư mục node_modules (dependencies)
node_modules/

# Bỏ qua file môi trường chứa thông tin nhạy cảm
.env

Backend/.env

# Bỏ qua lock file của pnpm (nếu dùng pnpm)
pnpm-lock.yaml

# Bỏ qua lock files khác (tùy package manager)
package-lock.json
yarn.lock

# Bỏ qua thư mục build/output
dist/
build/

# Bỏ qua log files và debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Bỏ qua thư mục temp và cache
tmp/
temp/
.cache/

# Bỏ qua file upload tạm thời (nếu có)
uploads/
public/uploads/
tmp/

# Bỏ qua IDE/editor config files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Bỏ qua thư mục node_modules (dependencies)
node_modules/

# Bỏ qua file môi trường chứa thông tin nhạy cảm
.env
>>>>>>> master

# Bỏ qua lock file của pnpm (nếu dùng pnpm)
pnpm-lock.yaml

# Bỏ qua lock files khác (tùy package manager)
package-lock.json
yarn.lock

# Bỏ qua thư mục build/output
dist/
build/

# Bỏ qua log files và debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Bỏ qua thư mục temp và cache
tmp/
temp/
.cache/

# Bỏ qua file upload tạm thời (nếu có)
uploads/
public/uploads/
tmp/

# Bỏ qua IDE/editor config files
.vscode/
.idea/
.DS_Store
Thumbs.db
